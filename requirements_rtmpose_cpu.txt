# RTMpose v2 CPU-Only 环境 requirements (优化顺序版本)
# 用法:
#   conda create -n pose python=3.10 -y
#   conda activate pose
#   pip install -r requirements_rtmpose_cpu.txt --prefer-binary --no-build-isolation
#   mim install mmcv
#   pip install mmpose==1.3.2 --prefer-binary --no-deps

# ============ 第1阶段：基础工具 ============
# 这些必须首先安装，为后续安装做准备
setuptools==60.2.0
wheel==0.47.0
openmim==0.3.9

# ============ 第2阶段：PyTorch（必须在 MMPose 之前）============
# 注意：使用 CPU 版本，避免 GPU 兼容性问题
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# ============ 第3阶段：核心框架 ============
Flask==3.1.3
Flask-CORS==6.0.5
Werkzeug==3.1.8

# ============ 第4阶段：MMPose 基础依赖 ============
mmengine==0.10.7
mmdet==3.2.0

# 注意：
# - mmcv 必须用 mim 安装（避免编译问题）: mim install mmcv
# - mmpose 在后面单独安装（避免 chumpy 编译）: pip install mmpose==1.3.2 --prefer-binary --no-deps

# ============ 第5阶段：计算和处理 ============
numpy==2.2.6
scipy==1.15.3
scikit-image==0.24.0

# ============ 第6阶段：图像处理 ============
opencv-python==5.0.0.93
Pillow==12.3.0
opencv-contrib-python==5.0.0.93

# ============ 第7阶段：数据和配置 ============
pandas==2.3.3
PyYAML==6.0.3

# ============ 第8阶段：工具库 ============
tqdm==4.65.2
requests==2.28.2
urllib3==1.26.20
certifi==2026.7.22

# ============ 第9阶段：回退方案（MediaPipe）============
mediapipe==0.10.14

# ============ 第10阶段：可视化（可选）============
matplotlib==3.10.9

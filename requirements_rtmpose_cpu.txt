# RTMpose v2 CPU-Only 环境 requirements
# 用法: conda create -n tenclip python=3.10 && conda activate tenclip && pip install -r requirements_rtmpose_cpu.txt

# ============ 核心框架 ============
Flask==3.1.3
Flask-CORS==6.0.5
Werkzeug==3.1.8

# ============ PyTorch CPU ============
# 注意：使用 CPU 版本，避免 GPU 兼容性问题
torch==2.5.1  # 使用 pip install 时会自动选择合适版本
torchvision==0.20.1
torchaudio==2.5.1

# ============ MMPose 相关 ============
mmengine==0.10.7
mmcv==2.1.0
mmdet==3.2.0
mmpose==1.3.2
openmim==0.3.9

# ============ 计算和处理 ============
numpy==2.2.6
scipy==1.15.3
scikit-image==0.24.0

# ============ 图像处理 ============
opencv-python==5.0.0.93
Pillow==12.3.0
opencv-contrib-python==5.0.0.93

# ============ 数据和配置 ============
pandas==2.3.3
PyYAML==6.0.3
json-tricks==3.17.0

# ============ 工具库 ============
tqdm==4.65.2
requests==2.28.2
urllib3==1.26.20
certifi==2026.7.22

# ============ 回退方案 (MediaPipe) ============
mediapipe==0.10.14

# ============ 可视化（可选） ============
matplotlib==3.10.9
seaborn==0.14.2

# ============ 开发工具（可选） ============
Cython==3.0.11
setuptools==60.2.0
wheel==0.47.0

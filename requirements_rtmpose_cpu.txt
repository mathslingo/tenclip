# RTMpose v2 CPU-Only 环境
#
# 云主机无 GPU 时，必须用 CPU 版 PyTorch 2.1。
# PyTorch 2.5 + CUDA 没有官方 mmcv wheel，mim 会强制源码编译并失败。
#
# 推荐安装顺序（不要只用一条 pip -r）：
#   conda create -n pose python=3.10 -y && conda activate pose
#   pip install setuptools==60.2.0 wheel==0.47.0
#   pip install torch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements_rtmpose_cpu.txt --prefer-binary
#   pip install mmcv==2.1.0 -f https://download.openmmlab.com/mmcv/dist/cpu/torch2.1.0/index.html
#   pip install mmpose==1.3.2 --prefer-binary --no-deps

# ============ 第1阶段：基础工具 ============
setuptools==60.2.0
wheel==0.47.0
openmim==0.3.9

# ============ 第2阶段：PyTorch（本文件不安装 torch）============
# 必须先从 CPU 源安装，避免装成 CUDA 版：
#   pip install torch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 --index-url https://download.pytorch.org/whl/cpu
# 阿里云镜像备选：
#   pip install torch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 -f https://mirrors.aliyun.com/pytorch-wheels/cpu/

# ============ 第3阶段：核心框架 ============
Flask==3.1.3
Flask-CORS==6.0.5
Werkzeug==3.1.8

# ============ 第4阶段：MMPose 基础依赖 ============
numpy==1.26.4
mmengine==0.10.7
mmdet==3.2.0

# mmcv / mmpose 不要写在这里：
# - mmcv 必须用官方 CPU wheel（见上方命令）
# - mmpose 用 --no-deps，避免编译 chumpy

# ============ 第5阶段：计算和处理 ============
scipy==1.15.3
scikit-image==0.24.0

# ============ 第6阶段：图像处理 ============
opencv-python==4.10.0.84
Pillow==10.4.0

# ============ 第7阶段：数据和配置 ============
pandas==2.2.3
PyYAML==6.0.2

# ============ 第8阶段：工具库 ============
tqdm==4.66.5
requests==2.32.3
urllib3==2.2.3
certifi==2024.8.30

# ============ 第9阶段：MMPose 运行时（不含 chumpy）============
json-tricks==3.17.3
munkres==1.1.4
xtcocotools==1.14.3
matplotlib==3.9.2

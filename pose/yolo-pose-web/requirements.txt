# pose/yolo-pose-web
#
# 推荐：conda activate mmpose_gpu 后只补缺包（通常只要 ultralytics）
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple 'ultralytics>=8.3.0,<9'
#   若 openxlab 报错：pip install 'filelock~=3.14.0'
#
# 可选独立 venv（不想动 conda 时）：
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# 浏览器推理不依赖本文件；仅 export_onnx / 训练网球模型需要。

ultralytics>=8.3.0,<9
torch>=2.1.0
torchvision>=0.16.0
onnx>=1.15.0,<2
onnxscript>=0.1.0
onnxslim>=0.1.31
onnxsim>=0.4.36

# 与 openxlab 共存时建议钉住：
# filelock~=3.14.0

onnxruntime>=1.17.0,<2
numpy>=1.24.0,<3
opencv-python-headless>=4.8.0,<5
Pillow>=10.0.0

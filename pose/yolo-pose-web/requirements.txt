# pose/yolo-pose-web — 请用独立 venv，勿装进 mmpose/openxlab 的 conda 环境
#
#   cd pose/yolo-pose-web
#   python3 -m venv .venv
#   source .venv/bin/activate          # Windows: .venv\Scripts\activate
#   pip install -U pip
#   pip install -r requirements.txt
#   python export_onnx.py
#   python download_assets.py
#
# 若已误装进共享环境并弄坏 openxlab：
#   pip install 'filelock~=3.14.0'

ultralytics>=8.3.0,<9
torch>=2.1.0
torchvision>=0.16.0
onnx>=1.15.0,<2
onnxscript>=0.1.0
onnxslim>=0.1.31
onnxsim>=0.4.36

# 与 openxlab 共存时再取消下一行注释；独立 venv 可忽略
# filelock~=3.14.0

onnxruntime>=1.17.0,<2
numpy>=1.24.0,<3
opencv-python-headless>=4.8.0,<5
Pillow>=10.0.0

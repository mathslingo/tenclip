name: tenclip_app
description: TenniTi / TenClip iOS & Android client (Flutter)
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1
  http_parser: ^4.1.2
  image_picker: ^1.1.2
  path: ^1.9.1
  path_provider: ^2.1.5
  video_player: ^2.9.5
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
